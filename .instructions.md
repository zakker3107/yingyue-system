---
applyTo: "yingyue-system/**/*.py"
---

# YingYue System Agent 指示文件

## 项目背景

YingYue System 是一个 AI 观察系统，采用四层循环架构：
1. **Scout Agent**: 新闻收集与初步分类
2. **Analyst Agent**: 信号分析与趋势识别
3. **Synthesizer Agent**: 洞见生成与知识组织
4. **API**: HTTP 接口供外部查询

## 关键约定

### 文件夹结构
```
agents/           # Scout, Analyst, Synthesizer Agent 实现
apps/             # API 和 Worker 应用
core/             # 领域驱动设计（domain/、orchestrator/、ports/）
config/           # JSON 配置（sources.json、philosophy_seed.json）
data/             # 数据（raw/、curated/yingyue.db、processed/reports/）
pipelines/        # 管道编排（orchestrator.py）和排程（scheduling/）
scripts/          # 可执行脚本（run_mvp.py、start_api.py 等）
services/         # API 和核心服务
tests/            # 测试代码
vendor/           # 第三方依赖
```

### 工作流程

**日常工作**：
1. 修改代码后，运行 `python scripts\optimize_environment.py --dev` 检查
2. 执行 `python scripts\run_mvp.py` 验证管道
3. 运行 `pytest` 确保测试通过
4. 使用 `python scripts\start_api.py` 进行 API 测试

**批处理脚本优先**：当用户要求快速执行时，优先建议使用 `.bat` 文件或 VS Code 任务，而不是直接命令

### 代码风格
- Python 3.10+ 风格（使用 `from __future__ import annotations`）
- Agent 必须继承 `agents/base.py` 中的 `BaseAgent`
- 所有业务逻辑应放在 `core/domain/` 或 `core/orchestrator/` 中
- API 端点在 `apps/api/` 中实现

### 数据输出
- 新闻数据存储在 `data/curated/yingyue.db`
- 日报告输出到 `data/processed/reports/daily_report_YYYY-MM-DD.md`
- 周报告输出到 `data/processed/reports/weekly_observation_YYYY-Www.md`
- 思想连结输出到 `data/processed/reports/thought_links_YYYY-MM-DD.md`

## 工作指南

### 当用户问"project 可以使用吗"
1. 验证环境：`python scripts\optimize_environment.py --dev`
2. 运行管道：`python scripts\run_mvp.py`
3. 测试 API：`python scripts\start_api.py` 并测试几个端点
4. 报告状态（是否可用）

### 当用户要求优化本地开发
1. 更新 `.vscode/tasks.json` 添加常用任务
2. 创建/更新 `.bat` 批处理脚本
3. 更新 README.md 和 AGENTS.md 文档
4. 验证所有脚本都能正常运行

### 当用户要求添加新功能
1. 确定该功能属于哪个 Agent（Scout/Analyst/Synthesizer）
2. 在 `agents/` 中实现或扩展相应 Agent
3. 在 `core/domain/` 中定义业务逻辑
4. 在 `pipelines/orchestrator.py` 中集成
5. 编写测试
6. 更新 AGENTS.md 文档

### 当用户遇到问题
1. 首先检查 `data/curated/yingyue.db` 是否存在
2. 运行 `python scripts\optimize_environment.py --full` 重建环境
3. 查看 `tests/test_pipeline_smoke.py` 的输出
4. 检查排程任务（`taskschd.msc` 中的 `YingYue-Daily-MVP`）

## 文档维护

- **README.md**: 快速开始、架构概览、常见问题
- **AGENTS.md**: Agent 工作流程、API 端点、配置详解
- **docs/automation.md**: 排程与自动化细节
- **.instructions.md** (本文件)**: 开发约定与工作流程

## 常用命令速查

```powershell
# 优化环境
python scripts\optimize_environment.py --dev

# 运行 MVP 管线
python scripts\run_mvp.py

# 启动 API
python scripts\start_api.py

# 运行测试
pytest
# 或
python tests\test_pipeline_smoke.py

# 安装排程
powershell -ExecutionPolicy Bypass -File "pipelines\scheduling\install_daily_task.ps1" -TaskName "YingYue-Daily-MVP" -Time "08:30"

# 移除排程
powershell -ExecutionPolicy Bypass -File "pipelines\scheduling\remove_daily_task.ps1" -TaskName "YingYue-Daily-MVP"
```

## 相关资源

- GitHub 仓库链接（如适用）
- 本地数据库：`data/curated/yingyue.db`
- 输出报告位置：`data/processed/reports/`
- API 默认端口：`8000`
- 排程执行时间：默认每日 `08:30`